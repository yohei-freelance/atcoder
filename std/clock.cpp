#include <bits/stdc++.h>
using namespace std;

int main(){
    // 実行にかかる時間を計測
    int ti = clock();
    for (int i = 1; i <= 100000; i++) cout << i << endl;
    printf("Execution Time: %.4lf sec\n", 1.0 * (clock() - ti) / CLOCKS_PER_SEC);
    return 0;
}