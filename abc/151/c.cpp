#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef string str;
using P = pair<int, int>;
const ll inf =1e9;

struct edge{
    int to;
    int cost;
};
using graph = vector<vector<edge>>;

int main(){
    int n, m; cin >> n >> m;
    vector<int> wa(m);
    vector<int> ac(m);
    for(int i=0; i<m; i++){
        int problem_num; cin >> problem_num;
        int problem_result; cin >> problem_result;
        if(problem_result == 'WA'){
            
        }
    }
}