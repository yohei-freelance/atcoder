#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef string str;

// bit全探索
void main(){
    int n; cin >> n;
    int ans = 0;
    vector<int> a(n);
    vector<vector<int>> testimony(n, vector<int>);
    REP(i, n){
        cin >> a[i];
        REP(j, a[i]){

            testimony[i].insert([])
        }
    }
    
}