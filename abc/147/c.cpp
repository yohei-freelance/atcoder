#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef string str;

// bit全探索
int main(){
    int n; cin >> n;
    vector<int> a(n);
    for(int i=0; i<n; i++){
        cin >> a[i];
        for(int j=0; j<a[i]; j++){
        }
    }
}