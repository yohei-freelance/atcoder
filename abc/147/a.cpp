#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef string str;
 
int main(){
    int a, b, c; cin >> a >> b >> c;
    if (a + b + c >= 22){
        printf("bust");
    }else{
        printf("win");
    }
  return 0;
}